#!/bin/sh

rm ${PBI_PROGDIRPATH}/lib/libstd*
rm ${PBI_PROGDIRPATH}/lib/libgcc*



if [ ! -z "${DISPLAY}" ]
then
else
  echo "Geekbench is installed! Geekbench is a command-line application, to use it run 'geekbench' from a shell prompt."
fi

